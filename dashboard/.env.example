# API URL for nginx to proxy requests to
# Format: http://hostname:port (no trailing slash)
# When using Docker network, use the coordinator service name
API_URL=http://coordinator:3001

# Dashboard port (optional, defaults to 8080)
DASHBOARD_PORT=8080
